# Repository tests

This directory tests monorepo-level governance and ownership rules. Component
tests remain with their owners:

- `shared/reference/tests/`: shared causal/statistical/contracts behavior;
- `causalperf-bench/tests/`: task packaging and leakage behavior;
- `causalperf-agent/tests/`: created when the first Agent implementation exists.
